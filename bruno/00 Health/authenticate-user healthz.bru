meta {
  name: authenticate-user healthz
  type: http
  seq: 3
}

get {
  url: {{authenticate_user_url}}/healthz
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("body says ok", function() {
    expect(res.getBody().status).to.equal("ok");
  });
}
