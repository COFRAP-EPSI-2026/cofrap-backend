meta {
  name: generate-password healthz
  type: http
  seq: 1
}

get {
  url: {{generate_password_url}}/healthz
  body: none
  auth: none
}

vars:pre-request {
  generate_password_url: http://192.168.1.240:80
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("body says ok", function() {
    expect(res.getBody().status).to.equal("ok");
  });
}
