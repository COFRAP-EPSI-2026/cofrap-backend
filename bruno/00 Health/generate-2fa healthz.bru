meta {
  name: generate-2fa healthz
  type: http
  seq: 2
}

get {
  url: {{generate_2fa_url}}/healthz
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("body says ok", function() {
    expect(res.getBody().status).to.equal("ok");
  });
}
