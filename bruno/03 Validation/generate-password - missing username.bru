meta {
  name: generate-password - missing username
  type: http
  seq: 2
}

post {
  url: {{generate_password_url}}
  body: json
  auth: none
}

body:json {
  {}
}

tests {
  test("status is 422", function() {
    expect(res.getStatus()).to.equal(422);
  });
}
