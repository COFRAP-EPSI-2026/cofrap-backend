meta {
  name: generate-password - empty username
  type: http
  seq: 1
}

post {
  url: {{generate_password_url}}
  body: json
  auth: none
}

body:json {
  {
    "username": ""
  }
}

tests {
  test("status is 422 (pydantic validation error)", function() {
    expect(res.getStatus()).to.equal(422);
  });
}
