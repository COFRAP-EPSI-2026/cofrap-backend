# Outillage de dev — installé en CI et en local.
# Les fonctions ont leurs propres requirements.txt (utilisés par les Dockerfile).

# Lint / format
ruff==0.16.0

# Tests
pytest==9.0.3
pytest-cov==7.1.0
httpx==0.28.1

# Dépendances applicatives — installées globalement pour exécuter les tests
fastapi==0.136.1
uvicorn[standard]==0.47.0
pydantic==2.13.4
pymysql==1.2.0
slowapi==0.1.9
cryptography==48.0.0
qrcode[pil]==8.2
pyotp==2.9.0

# Génération OpenAPI
pyyaml==6.0.3
