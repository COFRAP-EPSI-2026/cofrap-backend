# Outillage de dev — installé en CI et en local.
# Les fonctions ont leurs propres requirements.txt (utilisés par les Dockerfile).

# Lint / format
ruff==0.9.2

# Tests
pytest==8.3.4
pytest-cov==6.0.0
httpx==0.28.1            # TestClient FastAPI utilise httpx

# Dépendances applicatives — installées globalement pour exécuter les tests
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pymysql==1.1.1
cryptography==46.0.7
qrcode[pil]==8.0
pyotp==2.9.0

# Génération OpenAPI
pyyaml==6.0.2
